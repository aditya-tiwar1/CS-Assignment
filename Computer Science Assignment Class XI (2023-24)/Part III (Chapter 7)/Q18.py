# Program to calculate the radius of a sphere given its area
import math

area = float(input("Enter the area of the sphere: "))

radius = math.sqrt(area / (4 * math.pi))

print("Radius of the sphere:", radius)
# Output: Radius of the sphere: [calculated value]
